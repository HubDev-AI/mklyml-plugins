{
  "name": "@mklyml/plugins-seo",
  "private": true,
  "version": "0.1.0",
  "description": "SEO plugin for mkly — Open Graph, Twitter Cards, JSON-LD structured data",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@mklyml/core": "^0.2.0"
  },
  "devDependencies": {
    "typescript": "~5.8.2",
    "@types/bun": "latest"
  }
}
