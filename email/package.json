{
  "name": "@mklyml/plugins-email",
  "private": true,
  "version": "0.1.0",
  "description": "Email plugin for mkly — table-based HTML rendering for email clients",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@mklyml/core": "file:../../mkly"
  },
  "devDependencies": {
    "typescript": "~5.8.2",
    "@types/bun": "latest",
    "@mklyml/kits": "file:../../mkly-kits"
  }
}
